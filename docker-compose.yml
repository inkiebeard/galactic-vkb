services:
  postgres:
    image: pgvector/pgvector:pg17
    restart: unless-stopped
    environment:
      POSTGRES_DB: vkb
      POSTGRES_USER: vkb
      POSTGRES_PASSWORD: vkb
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U vkb -d vkb"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
